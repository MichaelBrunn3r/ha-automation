title: Home Assistant trigger
description: Fires when Home Assistant starts up or shuts down
type: object
properties:
  platform:
    const: homeassistant
  event:
    enum:
      - start
      - shutdown
required:
  - platform
  - event
