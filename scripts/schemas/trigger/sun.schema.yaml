title: Sun trigger
description:
  Fires when the sun is setting or rising, i.e., when the sun elevation
  reaches 0°
type: object
properties:
  platform:
    const: sun
  event:
    enum:
      - sunset
      - sunrise
  offset:
    $ref: "../defs.schema.yaml#/$defs/HHMMSS_offset"
required:
  - platform
  - event
