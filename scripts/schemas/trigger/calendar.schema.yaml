title: Calendar trigger
description: Calendar trigger fires when a Calendar event starts or ends, allowing
  much more flexible automations that using the Calendar entity state which only supports
  a single event start at a time
type: object
properties:
  platform:
    const: calendar
  event:
    enum:
      - start
      - end
  entity_id:
    $ref: "../defs.schema.json#/$defs/entity_id|entity_id[]"
  offset:
    $ref: "../defs.schema.json#/$defs/HHMMSS_offset"
required:
  - platform
  - event
  - entity_id
