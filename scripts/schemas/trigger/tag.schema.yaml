title: Tag trigger
description: Fires when a tag is scanned
type: object
properties:
  platform:
    const: tag
  tag_id:
    $ref: "../defs.schema.yaml#/$defs/str|str[]"
  device_id:
    $ref: "../defs.schema.yaml#/$defs/str|str[]"
  id:
    type: string
  enable:
    type: boolean
required:
  - platform
  - tag_id
