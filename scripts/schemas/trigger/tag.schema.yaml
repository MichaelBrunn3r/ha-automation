title: Tag trigger
description: Fires when a tag is scanned
type: object
properties:
  platform:
    const: tag
  tag_id:
    $ref: "../defs.schema.yaml#/$defs/str|str[]"
  device_id:
    $ref: "../defs.schema.yaml#/$defs/str|str[]"
required:
  - platform
  - tag_id
