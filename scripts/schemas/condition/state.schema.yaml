title: State Condition
description: Tests if an entity is a specified state
type: object
properties:
  condition:
    const: state
  entity_id:
    $ref: "../defs.schema.yaml#/$defs/entity_id|entity_id[]"
  attribute:
    type: string
  state:
    $ref: "../defs.schema.yaml#/$defs/str|str[]"
  match:
    const: any
  for:
    $ref: "../defs.schema.yaml#/$defs/time_period"
required:
  - condition
  - entity_id
  - state
